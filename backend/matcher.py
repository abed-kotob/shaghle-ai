class Matcher: #Takes job, worker and gives a score
    def __init__(self):
        return

    def compare(txt1, txt2): #Check the percentage similarity in two text files
        return  

    
    #One of the tasks is to match a job to a worker. 